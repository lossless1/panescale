---
phase: quick
plan: 260319-l62
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ipc.ts
  - src/components/canvas/FilePreviewNode.tsx
  - src/components/canvas/NoteNode.tsx
  - src/stores/canvasStore.ts
autonomous: true
requirements: [auto-edit-text, preview-md, skip-binary, render-images]
must_haves:
  truths:
    - "Text files (ts, js, py, rs, json, etc.) open in edit mode with editable textarea by default"
    - "Markdown files (.md, .mdx) open in preview mode (rendered markdown) by default"
    - "Binary files (zip, tar, gz, exe, dll, wasm, etc.) do not open at all when dragged to canvas"
    - "Image files (png, jpg, gif, svg, webp) render the image directly in the canvas tile"
  artifacts:
    - path: "src/lib/ipc.ts"
      provides: "extensionToTileType with binary detection returning null"
    - path: "src/components/canvas/FilePreviewNode.tsx"
      provides: "Editable text content with edit/preview toggle"
    - path: "src/stores/canvasStore.ts"
      provides: "addContentNode guards against null tileType (binary)"
  key_links:
    - from: "src/lib/ipc.ts"
      to: "src/stores/canvasStore.ts"
      via: "extensionToTileType return type includes null for binary"
      pattern: "extensionToTileType.*null"
    - from: "src/components/canvas/Canvas.tsx"
      to: "src/stores/canvasStore.ts"
      via: "addContentNode called only when tileType is not null"
      pattern: "tileType.*addContentNode"
---

<objective>
Make file tiles smarter about their default mode based on file type:
1. Text files open in edit mode (editable textarea) by default in FilePreviewNode
2. MD files stay in preview mode (rendered markdown) as they already do via NoteNode
3. Binary files (zip, tar, gz, exe, dll, wasm, etc.) are blocked from opening entirely
4. Image files already render correctly via ImageNode -- no changes needed

Purpose: Better UX -- users can immediately edit code files, read markdown, and are not confused by binary garbage on canvas.
Output: Modified ipc.ts, FilePreviewNode.tsx, canvasStore.ts, Canvas.tsx
</objective>

<execution_context>
@/Users/volodymyrsaakian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/volodymyrsaakian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/ipc.ts
@src/stores/canvasStore.ts
@src/components/canvas/Canvas.tsx
@src/components/canvas/FilePreviewNode.tsx
@src/components/canvas/NoteNode.tsx
@src/stores/fileDragStore.ts

<interfaces>
From src/lib/ipc.ts:
```typescript
export type ContentTileType = 'note' | 'image' | 'file-preview';

export function extensionToTileType(ext: string): ContentTileType {
  const lower = ext.toLowerCase();
  if (lower === '.md' || lower === '.mdx') return 'note';
  if (['.png', '.jpg', '.jpeg', '.gif', '.svg', '.webp', '.bmp'].includes(lower)) return 'image';
  return 'file-preview';
}
```

From src/stores/canvasStore.ts:
```typescript
addContentNode: (position: { x: number; y: number }, tileType: ContentTileType, fileData: { path: string; name: string }) => void;
```

From src/components/canvas/Canvas.tsx (mouseup handler for file drag):
```typescript
function onMouseUp(e: MouseEvent) {
  const { dragging, endDrag } = useFileDragStore.getState();
  if (!dragging) return;
  const position = reactFlow.screenToFlowPosition({ x: e.clientX, y: e.clientY });
  const tileType = extensionToTileType(dragging.ext);
  addContentNode(position, tileType, dragging);
  endDrag();
}
```

From FilePreviewNode: Currently read-only with shiki syntax highlighting. No edit mode.
From NoteNode: Has isPreview toggle, file-backed notes load with isPreview: true (preview mode). Has Edit/Preview button.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add binary detection to extensionToTileType and guard addContentNode</name>
  <files>src/lib/ipc.ts, src/stores/canvasStore.ts, src/components/canvas/Canvas.tsx</files>
  <action>
  1. In `src/lib/ipc.ts`:
     - Change `ContentTileType` to: `export type ContentTileType = 'note' | 'image' | 'file-preview';`
     - Add a new constant `BINARY_EXTENSIONS` as a Set containing common binary extensions: `.zip`, `.tar`, `.gz`, `.tgz`, `.bz2`, `.xz`, `.7z`, `.rar`, `.exe`, `.dll`, `.so`, `.dylib`, `.wasm`, `.bin`, `.o`, `.obj`, `.class`, `.pyc`, `.pyo`, `.dmg`, `.iso`, `.img`, `.pkg`, `.deb`, `.rpm`, `.msi`, `.app`, `.jar`, `.war`, `.ear`, `.pdf`, `.doc`, `.docx`, `.xls`, `.xlsx`, `.ppt`, `.pptx`, `.ttf`, `.otf`, `.woff`, `.woff2`, `.eot`, `.ico`, `.mp3`, `.mp4`, `.avi`, `.mov`, `.mkv`, `.flac`, `.wav`, `.ogg`, `.aac`, `.sqlite`, `.db`
     - Change `extensionToTileType` return type to `ContentTileType | null`
     - Add binary check FIRST in the function: `if (BINARY_EXTENSIONS.has(lower)) return null;`

  2. In `src/components/canvas/Canvas.tsx`:
     - In the `onMouseUp` handler inside the file drag useEffect, after computing `tileType`, add a guard: `if (!tileType) { endDrag(); return; }` -- this prevents binary files from creating tiles.

  3. In `src/stores/canvasStore.ts`:
     - Update `addContentNode` type signature to accept `tileType: ContentTileType` (no null -- callers should guard before calling). No functional change needed here since callers will guard.
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Binary files (zip, tar, exe, etc.) are silently ignored when dragged to canvas. extensionToTileType returns null for binary extensions.</done>
</task>

<task type="auto">
  <name>Task 2: Add edit mode to FilePreviewNode with edit/preview toggle</name>
  <files>src/components/canvas/FilePreviewNode.tsx</files>
  <action>
  Transform FilePreviewNode from read-only to an editable tile that defaults to edit mode:

  1. Add state: `const [isEditing, setIsEditing] = useState(true)` -- default to edit mode for text files.
  2. Add a `writeTextFile` import from `@tauri-apps/plugin-fs` alongside the existing `readTextFile`.
  3. Add an `editContent` state (separate from `content`) initialized from loaded content. This tracks unsaved edits.
  4. Add a `dirty` derived flag: `const dirty = editContent !== content`.
  5. Add a save handler that calls `writeTextFile(filePath, editContent)` and updates `content` to match `editContent`. Bind to Cmd/Ctrl+S while the textarea is focused (use a keydown handler on the textarea element).
  6. In the title bar (drag-handle div), add an Edit/Preview toggle button (same style as NoteNode's toggle -- small border button, right-aligned). When in edit mode show "Preview", when in preview show "Edit".
  7. Also show a small dot or indicator next to the filename when `dirty` is true (unsaved changes).
  8. When `isEditing` is true, render a `<textarea>` with the following styles:
     - `className="nodrag nowheel nopan"` on the textarea
     - Font: JetBrains Mono / Fira Code monospace, 12px, line-height 1.5
     - Background transparent, color var(--text-primary)
     - width 100%, height 100%, no border, resize none, padding 8px
     - tabSize 2, whiteSpace pre, overflow auto
     - Spellcheck off
     - Value bound to `editContent`, onChange updates `editContent`
  9. When `isEditing` is false (preview mode), show the existing syntax-highlighted content (current behavior).
  10. Sync: when content loads from file (the readTextFile effect), also set `editContent` to the loaded text.
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>FilePreviewNode opens in edit mode by default with a working textarea. Toggle button switches between edit and syntax-highlighted preview. Cmd+S saves changes. Dirty indicator shown when unsaved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. App builds: `npm run build` succeeds
3. Manual: Drag a .ts file to canvas -- opens in edit mode with textarea
4. Manual: Drag a .md file to canvas -- opens as NoteNode in preview mode (existing behavior, unchanged)
5. Manual: Drag a .zip file to canvas -- nothing happens (file is silently ignored)
6. Manual: Drag a .png file to canvas -- renders image (existing behavior, unchanged)
7. Manual: In FilePreviewNode, click Preview button -- shows syntax-highlighted code
8. Manual: In FilePreviewNode, edit text, see dirty dot, press Cmd+S to save
</verification>

<success_criteria>
- Text files default to editable textarea in FilePreviewNode
- MD files use NoteNode with preview mode (unchanged)
- Binary files are blocked from canvas tile creation
- Image files render as images via ImageNode (unchanged)
- Edit/Preview toggle works in FilePreviewNode
- Cmd+S saves edited content to disk
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260319-l62-auto-edit-text-files-preview-md-skip-bin/260319-l62-SUMMARY.md`
</output>
