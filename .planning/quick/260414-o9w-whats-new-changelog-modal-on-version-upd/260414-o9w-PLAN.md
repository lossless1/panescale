---
phase: quick
plan: 260414-o9w
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/changelog.ts
  - src/components/layout/WhatsNewModal.tsx
  - src/components/layout/AppShell.tsx
autonomous: true
requirements: ["whats-new-changelog-modal"]

must_haves:
  truths:
    - "Modal appears automatically after app updates to a new version"
    - "Modal shows changelog entries for the current version"
    - "Modal only appears once per version (not on every launch)"
    - "User can dismiss the modal and it won't reappear for that version"
  artifacts:
    - path: "src/lib/changelog.ts"
      provides: "Changelog data and version-seen tracking logic"
    - path: "src/components/layout/WhatsNewModal.tsx"
      provides: "What's New modal component"
    - path: "src/components/layout/AppShell.tsx"
      provides: "Mounts WhatsNewModal"
  key_links:
    - from: "src/components/layout/WhatsNewModal.tsx"
      to: "src/lib/changelog.ts"
      via: "import changelog data and version tracking"
      pattern: "import.*changelog"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/layout/WhatsNewModal.tsx"
      via: "renders WhatsNewModal"
      pattern: "<WhatsNewModal"
---

<objective>
Add a "What's New" changelog modal that automatically displays after the app updates to a new version, showing recent changes and features. The modal uses localStorage to track which version the user last saw, so it only appears once per version update.

Purpose: Inform users about new features and changes after updating Panescale.
Output: WhatsNewModal component, changelog data module, integrated into AppShell.
</objective>

<execution_context>
@/Users/volodymyrsaakian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/volodymyrsaakian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/layout/AppShell.tsx
@src/components/layout/SettingsModal.tsx
@src/components/layout/UpdateToast.tsx
@src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create changelog data module and version tracking</name>
  <files>src/lib/changelog.ts</files>
  <action>
Create `src/lib/changelog.ts` with:

1. A `ChangelogEntry` interface: `{ version: string; date: string; items: { type: "feature" | "fix" | "improvement"; text: string }[] }`.

2. A `CHANGELOG` array of `ChangelogEntry` objects, ordered newest-first. Populate with entries for version 0.2.0 (current). Include 5-8 representative items covering recent features like workspaces, project organization, terminal bell sounds, inline rename, keyboard shortcuts for terminal cycling, etc. — drawn from the quick tasks completed in STATE.md.

3. A `getUnseenChangelog()` function that:
   - Reads `localStorage.getItem("panescale-last-seen-version")`
   - If null (fresh install or first time), sets the key to the current app version and returns null (don't show modal on first install)
   - If the stored version matches the latest changelog entry version, returns null
   - Otherwise returns the changelog entries newer than the stored version

4. A `markChangelogSeen()` function that sets `localStorage.setItem("panescale-last-seen-version", CHANGELOG[0].version)`

5. Export `CHANGELOG`, `ChangelogEntry`, `getUnseenChangelog`, `markChangelogSeen`.

Use the app version from `CHANGELOG[0].version` (not Tauri API) to keep it simple and avoid async initialization.
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/panescale && npx tsc --noEmit src/lib/changelog.ts 2>&1 | head -20</automated>
  </verify>
  <done>changelog.ts exports ChangelogEntry type, CHANGELOG array, getUnseenChangelog(), and markChangelogSeen(). Type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create WhatsNewModal component and wire into AppShell</name>
  <files>src/components/layout/WhatsNewModal.tsx, src/components/layout/AppShell.tsx</files>
  <action>
Create `src/components/layout/WhatsNewModal.tsx`:

1. On mount, call `getUnseenChangelog()`. If it returns entries, open the modal. Otherwise render nothing.

2. Modal design — follow the same visual patterns as SettingsModal.tsx:
   - Fixed overlay with rgba(0,0,0,0.5) backdrop, click-outside to close
   - Centered card with `var(--bg-secondary)` background, `var(--border)` border, 12px border radius, 24px padding, max-width 520px, max-height 80vh with overflow-y auto
   - Close button (x) in top-right corner, same style as SettingsModal
   - Title: "What's New in Panescale" in h2, fontSize 18, fontWeight 600, color var(--text-primary)
   - Subtitle showing the version number in var(--text-secondary), fontSize 13

3. For each changelog entry, render:
   - Version + date header (fontSize 14, fontWeight 600, var(--text-primary), with date in var(--text-secondary))
   - List of items with colored type badges:
     - "feature" badge: background #4a6cf7 (the accent blue used elsewhere)
     - "fix" badge: background #22c55e (green)
     - "improvement" badge: background #f59e0b (amber)
   - Badge style: fontSize 10, fontWeight 600, color #fff, padding 2px 6px, borderRadius 4px, uppercase, inline before item text
   - Item text: fontSize 13, color var(--text-primary), marginLeft 8px

4. "Got it" button at bottom: full width, var(--accent) background, white text, 6px 12px padding, borderRadius 6, fontSize 13, fontWeight 500. On click: call `markChangelogSeen()` and close modal.

5. Escape key closes the modal (same pattern as SettingsModal useEffect).

6. On close (via button, backdrop click, or Escape), always call `markChangelogSeen()` so the modal won't reappear.

Then modify `src/components/layout/AppShell.tsx`:
- Import `WhatsNewModal` from `./WhatsNewModal`
- Render `<WhatsNewModal />` inside the AppShell, after `<UpdateToast />` (at the end of the outer div)
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/panescale && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>WhatsNewModal renders changelog entries in a styled modal. AppShell mounts it. Full project type-checks. Modal auto-shows on version change, marks seen on dismiss, does not reappear on subsequent launches.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Manual: clear `panescale-last-seen-version` from localStorage, reload app — modal should appear
- Manual: close modal, reload — modal should NOT reappear
- Manual: set `panescale-last-seen-version` to an older version in localStorage, reload — modal should appear
</verification>

<success_criteria>
- WhatsNewModal displays changelog entries for the current version
- Modal only shows once per version update (localStorage tracking)
- Modal follows existing SettingsModal visual patterns (consistent UI)
- Fresh installs do not trigger the modal
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260414-o9w-whats-new-changelog-modal-on-version-upd/260414-o9w-SUMMARY.md`
</output>
