---
phase: quick-260410-f2f
plan: 01
files_modified:
  - src/stores/projectStore.ts
  - src/components/sidebar/ProjectsList.tsx
autonomous: true
---

<objective>
Add drag-and-drop reordering of projects in the Projects sidebar tab. Mirror the existing @dnd-kit pattern used in `TerminalList.tsx` so the behavior is consistent across the app.
</objective>

<tasks>
- Add `reorderProjects(fromPath, toPath)` to `useProjectStore`, persisted via the existing zustand `persist` middleware. It must keep the active project pointing at the same project after reorder.
- Refactor `ProjectsList.tsx`:
  - Extract each row into a `SortableProjectRow` component using `useSortable({ id: project.path })`
  - Wrap the list in `DndContext` + `SortableContext` with `verticalListSortingStrategy` and a vertical-only modifier
  - Require a 5px activation distance so single clicks still toggle expansion
  - Stop pointer propagation on the close-x button so drag doesn't fire when closing a project
</tasks>

<verification>
- `npx tsc --noEmit` passes
- Manual: drag a project row to a different position — order updates and persists across reload
- Manual: clicking a project header (no drag) still toggles expansion and sets active
- Manual: clicking the x button closes the project without starting a drag
</verification>
