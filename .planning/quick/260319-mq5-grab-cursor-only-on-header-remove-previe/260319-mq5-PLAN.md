---
phase: quick
plan: 260319-mq5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/canvas/FilePreviewNode.tsx
  - src/components/canvas/Canvas.tsx
autonomous: true
---

<objective>
Fix grab cursor scope and remove preview mode from FilePreviewNode.

1. Grab/hand cursor should only appear on the title bar header (`.drag-handle`), not the entire tile body. React Flow applies `cursor: grab` to `.react-flow__node` by default -- override it to `cursor: default` for all nodes, keeping `cursor: grab` only on `.drag-handle` elements.
2. Remove preview mode from FilePreviewNode -- always show CodeMirror editor. Delete the Preview/Edit toggle button and all preview/shiki-related state and logic.
3. NoteNode keeps its Preview/Edit toggle unchanged (no modifications needed).

Purpose: Improve UX -- code files should always be editable, and the grab cursor should only appear where dragging is possible.
Output: Cleaner FilePreviewNode component, correct cursor behavior across all tile types.
</objective>

<context>
@src/components/canvas/FilePreviewNode.tsx
@src/components/canvas/Canvas.tsx
@src/components/canvas/NoteNode.tsx (reference only -- no changes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Override node cursor and remove preview mode from FilePreviewNode</name>
  <files>src/components/canvas/FilePreviewNode.tsx, src/components/canvas/Canvas.tsx</files>
  <action>
**Canvas.tsx -- Add CSS override for node cursor (in the existing `<style>` block around line 422):**
Add a rule to reset the default React Flow node cursor:
```css
.react-flow__node {
  cursor: default !important;
}
```
This ensures only `.drag-handle` elements (which already have inline `cursor: grab`) show the grab cursor. All other node areas get `cursor: default`. The `.react-flow__pane` rules remain unchanged.

**FilePreviewNode.tsx -- Remove preview mode, always show CodeMirror editor:**

1. **Remove imports:** Remove `getHighlighter`, `loadedLangs`, `detectLanguage` from shikiHighlighter import. Keep `detectLanguage` (used for language detection for CodeMirror). Actually `detectLanguage` is from shikiHighlighter -- keep that single import.

2. **Remove state variables:** Delete `isEditing` (line 47), `highlightedHtml` (line 48), `loading` (line 50). Keep `content`, `editContent`, `error`. Keep `mountedRef`.

3. **Remove the shiki highlight useEffect** (lines 96-133 entirely -- the one that calls `getHighlighter` and sets `highlightedHtml`).

4. **Update the loading state:** Remove `setLoading(true)` and `setLoading(false)` from the file load useEffect (lines 63-79). Instead, use a simpler `loaded` boolean: start `false`, set `true` when readTextFile resolves. Show "Loading..." until loaded, then always show CodeEditor.

5. **Remove the Preview/Edit toggle button** (lines 216-231 -- the button with `{isEditing ? "Preview" : "Edit"}`). Keep the close button.

6. **Simplify the body content area** (lines 258-325). Replace the 4-branch ternary (`error ? ... : loading ? ... : isEditing ? ... : highlightedHtml ? ... : ...`) with just 2 branches:
   - If `error`: show error message
   - Else if not loaded: show "Loading..."
   - Else: show `<CodeEditor>` with `value={editContent}`, `onChange={setEditContent}`, `onSave={handleSave}`, `language={detectLanguage(fileName)}`, `theme={resolvedTheme === "light" ? "light" : "dark"}`

7. **Clean up unused imports:** Remove `getHighlighter` and `loadedLangs` from the shikiHighlighter import (keep `detectLanguage`). If shikiHighlighter import only has `detectLanguage`, update accordingly. The `import "../../styles/codemirror.css"` stays.
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- FilePreviewNode always renders CodeMirror editor (no preview/edit toggle, no shiki highlighting)
- Grab cursor only appears on `.drag-handle` title bar headers across all node types
- TypeScript compiles without errors
- NoteNode unchanged (still has Preview/Edit toggle)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- FilePreviewNode.tsx has no references to `isEditing`, `highlightedHtml`, `loading`, `getHighlighter`, or `loadedLangs`
- FilePreviewNode.tsx still imports and renders `CodeEditor`
- Canvas.tsx contains `.react-flow__node { cursor: default !important; }`
- NoteNode.tsx is unmodified
</verification>

<success_criteria>
- Code file tiles always show the CodeMirror editor immediately after loading
- No Preview/Edit toggle button visible on code file tiles
- Grab cursor appears only when hovering over the title bar header on any tile type
- NoteNode retains its existing Preview/Edit toggle behavior
</success_criteria>

<output>
After completion, create `.planning/quick/260319-mq5-grab-cursor-only-on-header-remove-previe/260319-mq5-SUMMARY.md`
</output>
