---
phase: quick
plan: 260319-lc5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shikiHighlighter.ts
  - src/components/canvas/FilePreviewNode.tsx
  - src/components/canvas/NoteNode.tsx
  - src/styles/globals.css
autonomous: true
must_haves:
  truths:
    - "FilePreviewNode preview mode renders syntax-highlighted code with correct theme (dark or light)"
    - "Both FilePreviewNode and NoteNode share a single shiki highlighter instance"
    - "Shiki output has proper CSS styling (no double backgrounds, correct padding)"
  artifacts:
    - path: "src/lib/shikiHighlighter.ts"
      provides: "Shared shiki highlighter module with getHighlighter and loadedLangs"
    - path: "src/components/canvas/FilePreviewNode.tsx"
      provides: "Theme-aware syntax highlighting in preview mode"
    - path: "src/styles/globals.css"
      provides: "CSS for shiki-generated HTML in file preview"
  key_links:
    - from: "src/components/canvas/FilePreviewNode.tsx"
      to: "src/lib/shikiHighlighter.ts"
      via: "import getHighlighter, loadedLangs"
    - from: "src/components/canvas/NoteNode.tsx"
      to: "src/lib/shikiHighlighter.ts"
      via: "import getHighlighter, loadedLangs"
---

<objective>
Improve FilePreviewNode shiki integration: extract shared highlighter, add theme awareness, and add CSS styling.

Purpose: FilePreviewNode already has shiki syntax highlighting but it hardcodes "one-dark-pro" theme (ignoring light mode), duplicates the highlighter cache from NoteNode, and lacks CSS for the shiki-generated HTML. Fix all three.
Output: Shared highlighter module, theme-aware preview, proper shiki CSS.
</objective>

<execution_context>
@/Users/volodymyrsaakian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/volodymyrsaakian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/canvas/FilePreviewNode.tsx
@src/components/canvas/NoteNode.tsx
@src/styles/globals.css
@src/stores/themeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared shiki highlighter module and update both consumers</name>
  <files>src/lib/shikiHighlighter.ts, src/components/canvas/FilePreviewNode.tsx, src/components/canvas/NoteNode.tsx</files>
  <action>
1. Create `src/lib/shikiHighlighter.ts` extracting the shared highlighter pattern:
   - Move the module-level `highlighterPromise`, `loadedLangs` Set, and `getHighlighter` function from NoteNode.tsx (it has the better version with COMMON_LANGS preload).
   - Also export `loadedLangs` (read-only, needed by both consumers to check if a lang is available).
   - Also export the `COMMON_LANGS` array and `EXT_LANG_MAP` + `detectLanguage` from FilePreviewNode (these are useful shared utilities).
   - Export: `getHighlighter`, `loadedLangs`, `detectLanguage`, `EXT_LANG_MAP`.

2. Update `FilePreviewNode.tsx`:
   - Remove the duplicate `highlighterPromise`, `loadedLangs`, `getHighlighter`, `createHighlighter` import, `EXT_LANG_MAP`, and `detectLanguage` -- import them from `../../lib/shikiHighlighter`.
   - Remove `import { type Highlighter, createHighlighter } from "shiki"` (no longer needed directly).
   - Add `import { useThemeStore } from "../../stores/themeStore"` for theme awareness.
   - In the component, get resolved theme: `const resolvedTheme = useThemeStore((s) => s.resolvedTheme)`.
   - In the highlight useEffect (around line 166-199), use `resolvedTheme === "light" ? "github-light" : "one-dark-pro"` instead of hardcoded `"one-dark-pro"`.
   - Add `resolvedTheme` to the useEffect dependency array so highlighting re-runs on theme change.

3. Update `NoteNode.tsx`:
   - Remove the duplicate `highlighterPromise`, `loadedLangs`, `getHighlighter`, `COMMON_LANGS`, and `createHighlighter` import.
   - Import `getHighlighter`, `loadedLangs` from `../../lib/shikiHighlighter`.
   - Keep the `extractCodeLanguages` function local (it is NoteNode-specific for markdown parsing).
   - Everything else stays the same.
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Single shared highlighter module used by both FilePreviewNode and NoteNode. FilePreviewNode respects light/dark theme. No duplicate highlighter caches.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS styling for shiki-generated HTML in file preview</name>
  <files>src/styles/globals.css</files>
  <action>
Add CSS rules to globals.css for the `.shiki-container` class used in FilePreviewNode. The shiki library generates `<pre class="shiki ..."><code>` HTML. Style it so:

```css
/* Shiki syntax highlighting in file preview tiles */
.shiki-container .shiki {
  margin: 0;
  padding: 8px;
  background: transparent !important; /* Let container handle background */
  tab-size: 2;
}

.shiki-container .shiki code {
  font-family: 'JetBrains Mono', 'Fira Code', monospace;
  font-size: 12px;
  line-height: 1.5;
}
```

Place these rules after the existing `.prose` block (around line 175+).
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && grep -c "shiki-container" src/styles/globals.css</automated>
  </verify>
  <done>Shiki-generated HTML in FilePreviewNode has consistent styling: no double backgrounds, proper font and spacing.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Only one `highlighterPromise` exists in the codebase (in `src/lib/shikiHighlighter.ts`)
- FilePreviewNode imports `useThemeStore` and uses `resolvedTheme` for theme selection
- `.shiki-container .shiki` CSS rules exist in globals.css
</verification>

<success_criteria>
- FilePreviewNode preview mode uses the correct shiki theme based on user's light/dark preference
- Both NoteNode and FilePreviewNode share a single highlighter instance (no duplicate module-level caches)
- Shiki-generated HTML is properly styled with transparent background and consistent font
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/quick/260319-lc5-add-shiki-syntax-highlighting-to-filepre/260319-lc5-SUMMARY.md`
</output>
