---
phase: quick-260410-f4u
plan: 01
files_modified:
  - src/lib/ipc.ts
  - src/stores/projectStore.ts
  - src/stores/workspacesStore.ts
  - src/components/layout/Sidebar.tsx
autonomous: true
---

<objective>
1. Make the Projects list per-workspace so each workspace has its own open projects. Creating a new workspace yields an empty Projects tab; switching between workspaces restores each workspace's own projects.
2. Add a visible rename button (pencil icon) to each workspace row in the dropdown (alongside the existing hidden dblclick shortcut).
</objective>

<tasks>
- Add `projects: WorkspaceProject[]` and `activeProjectIndex: number` to the `Workspace` interface in `ipc.ts` (with a new `WorkspaceProject` type mirroring the projectStore shape).
- Remove `projects`/`activeProjectIndex` from the projectStore persist middleware — they now live inside the active workspace. Keep `viewMode` globally persisted.
- Hook projectStore mutations (`openProject`, `openRemoteProject`, `closeProject`, `setActiveProject`, `reorderProjects`) to call a lazy-imported `schedulePersist()` that triggers `useWorkspacesStore.persistActiveSnapshot()` via `queueMicrotask`, avoiding circular deps.
- workspacesStore: in `hydrate`, `switchWorkspace`, and `createWorkspace`, populate `useProjectStore.setState({ projects, activeProjectIndex })` from the target workspace. In `persistActiveSnapshot`, serialize current projects into the active workspace.
- Sidebar workspaces dropdown: add a pencil-icon rename button before the delete x, using the existing `renameWorkspace` action. Keep the dblclick shortcut too.
</tasks>

<verification>
- `npx tsc --noEmit` passes
- Manual: in Workspace 1, open 2 projects. Create Workspace 2 — Projects tab is empty. Open a different project in Workspace 2, switch back to Workspace 1 — original 2 projects return. Quit and relaunch — state survives.
- Manual: click the new pencil icon on a workspace row → prompt appears → rename persists.
</verification>
