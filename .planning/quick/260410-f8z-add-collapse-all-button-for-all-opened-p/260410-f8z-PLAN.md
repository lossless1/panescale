---
phase: quick-260410-f8z
plan: 01
files_modified:
  - src/components/sidebar/ProjectsList.tsx
autonomous: true
---

<objective>
Add a toggle button at the top of the Projects tab that collapses all expanded project sections with one click, and expands them all again on the next click.
</objective>

<tasks>
- Add a compact toolbar row above the projects list (same pattern as the TerminalList toolbar) with a single icon button on the right
- Button toggles between "collapse all" (when at least one project is expanded) and "expand all" (when all are collapsed)
- Icon swaps based on state — outward chevrons when all collapsed (click to expand), inward chevrons when any expanded (click to collapse)
- Use the existing `expanded` Set state in `ProjectsList`
</tasks>

<verification>
- `npx tsc --noEmit` passes
- Manual: with ≥1 project expanded, click button → all collapse; icon flips; click again → all expand
</verification>
