---
phase: 02-sidebar-session-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/themeStore.ts
  - src/stores/settingsStore.ts
  - src/lib/terminalSchemes.ts
  - src/styles/themes.ts
  - src/components/canvas/TerminalNode.tsx
  - src-tauri/tauri.conf.json
  - src/App.tsx
autonomous: true
requirements:
  - THEM-02
  - THEM-03
  - THEM-04

must_haves:
  truths:
    - "App detects system dark/light preference and applies it on launch without flicker"
    - "User can choose System/Dark/Light theme preference (3 options)"
    - "Terminal tiles render with One Dark color scheme by default"
    - "User can switch terminal color scheme to Dracula in settings"
    - "App window has rounded corners on macOS"
  artifacts:
    - path: "src/stores/themeStore.ts"
      provides: "Three-mode theme preference with system detection"
      exports: ["useThemeStore"]
      contains: "ThemePreference"
    - path: "src/lib/terminalSchemes.ts"
      provides: "One Dark and Dracula xterm.js ITheme presets"
      exports: ["terminalSchemes"]
    - path: "src/stores/settingsStore.ts"
      provides: "Terminal color scheme selection"
      contains: "colorScheme"
  key_links:
    - from: "src/stores/themeStore.ts"
      to: "window.matchMedia"
      via: "prefers-color-scheme listener"
      pattern: "prefers-color-scheme"
    - from: "src/lib/terminalSchemes.ts"
      to: "src/components/canvas/TerminalNode.tsx"
      via: "ITheme applied to xterm instance"
      pattern: "terminalSchemes"
---

<objective>
Extend the theme system to support system/dark/light preference with auto-detection, add terminal color scheme presets (One Dark + Dracula), and enable rounded window corners on macOS.

Purpose: Polishes the visual experience with system-aware theming, professional terminal colors, and native-feeling rounded corners.
Output: Three-mode theme toggle, two terminal color schemes, rounded macOS window.
</objective>

<execution_context>
@/Users/volodymyrsaakian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/volodymyrsaakian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sidebar-session-persistence/02-CONTEXT.md
@.planning/phases/02-sidebar-session-persistence/02-RESEARCH.md

<interfaces>
From src/stores/themeStore.ts:
```typescript
type ThemeName = "dark" | "light";
interface ThemeState {
  theme: ThemeName;
  toggleTheme: () => void;
  setTheme: (theme: ThemeName) => void;
}
// Uses localStorage for persistence (key: "excalicode-theme")
```

From src/stores/settingsStore.ts:
```typescript
interface SettingsState {
  fontFamily: string;
  fontSize: number;
  scrollback: number;
  // ... setters
}
```

From src/styles/themes.ts:
```typescript
export type ThemeName = "dark" | "light";
export type ThemeVariables = Record<string, string>;
export const themes: Record<ThemeName, ThemeVariables>;
// CSS variables: --bg-primary, --bg-secondary, --bg-titlebar, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-mode theme + terminal color schemes</name>
  <files>
    src/stores/themeStore.ts,
    src/stores/settingsStore.ts,
    src/lib/terminalSchemes.ts,
    src/styles/themes.ts
  </files>
  <read_first>
    src/stores/themeStore.ts,
    src/stores/settingsStore.ts,
    src/styles/themes.ts,
    src/components/canvas/TerminalNode.tsx,
    .planning/phases/02-sidebar-session-persistence/02-RESEARCH.md
  </read_first>
  <action>
1. Create `src/lib/terminalSchemes.ts` with One Dark and Dracula ITheme presets exactly as specified in RESEARCH.md:
   - Export `type TerminalSchemeName = 'one-dark' | 'dracula'`
   - Export `const terminalSchemes: Record<TerminalSchemeName, ITheme>` with full ANSI color palettes
   - Import `ITheme` from `@xterm/xterm`
   - One Dark: background #282C34, foreground #ABB2BF, cursor #528BFF (full palette from research)
   - Dracula: background #282A36, foreground #F8F8F2, cursor #F8F8F2 (full palette from research)

2. Update `src/stores/themeStore.ts`:
   - Change `ThemeName` to `ResolvedTheme` (keep "dark" | "light")
   - Add `type ThemePreference = 'system' | 'dark' | 'light'`
   - Change state shape:
     ```typescript
     interface ThemeState {
       preference: ThemePreference;
       resolvedTheme: ResolvedTheme;
       setPreference: (pref: ThemePreference) => void;
     }
     ```
   - Add `getSystemTheme()` function: `window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'`
   - Add `resolveTheme(pref)` function: returns system theme if 'system', otherwise the preference
   - Initialize: Read preference from localStorage (key "excalicode-theme-pref", default "system"). Resolve immediately using `getSystemTheme()` (synchronous, no flicker).
   - Persist preference to localStorage on change.
   - On store creation, add `matchMedia('(prefers-color-scheme: dark)')` event listener that updates `resolvedTheme` when preference is 'system'.
   - Remove old `toggleTheme` (replace with `setPreference`).
   - Keep backward compatibility: export `resolvedTheme` as `theme` alias for existing consumers. Update ThemeName export in themes.ts to be `ResolvedTheme`.

3. Update `src/styles/themes.ts`:
   - Change `ThemeName` export to `ResolvedTheme` type name (or keep ThemeName as alias)
   - No CSS variable changes needed -- existing dark/light themes are fine

4. Update `src/stores/settingsStore.ts`:
   - Add `colorScheme: TerminalSchemeName` field (default: 'one-dark')
   - Add `setColorScheme: (scheme: TerminalSchemeName) => void`
   - Import `TerminalSchemeName` from `../lib/terminalSchemes`
   - Add zustand persist middleware (key: "excalicode-settings") to persist all settings including colorScheme

5. Find all consumers of `useThemeStore` that reference `.theme` and ensure they work with the new `resolvedTheme` field. The main consumer is likely `App.tsx` which applies CSS variables. Either:
   - Keep `.theme` as an alias for `.resolvedTheme` in the store, OR
   - Update App.tsx to use `.resolvedTheme`
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'ThemePreference' src/stores/themeStore.ts
    - grep -q 'system' src/stores/themeStore.ts
    - grep -q 'prefers-color-scheme' src/stores/themeStore.ts
    - grep -q 'resolveTheme' src/stores/themeStore.ts
    - grep -q 'one-dark' src/lib/terminalSchemes.ts
    - grep -q 'dracula' src/lib/terminalSchemes.ts
    - grep -q '#282C34' src/lib/terminalSchemes.ts
    - grep -q '#282A36' src/lib/terminalSchemes.ts
    - grep -q 'colorScheme' src/stores/settingsStore.ts
    - grep -q 'TerminalSchemeName' src/stores/settingsStore.ts
  </acceptance_criteria>
  <done>ThemeStore supports System/Dark/Light preference with matchMedia listener. SettingsStore has colorScheme selection. terminalSchemes.ts exports One Dark and Dracula ITheme presets.</done>
</task>

<task type="auto">
  <name>Task 2: Apply color scheme to terminals + rounded corners</name>
  <files>
    src/components/canvas/TerminalNode.tsx,
    src-tauri/tauri.conf.json,
    src/App.tsx
  </files>
  <read_first>
    src/components/canvas/TerminalNode.tsx,
    src/App.tsx,
    src-tauri/tauri.conf.json,
    src/lib/terminalSchemes.ts,
    src/stores/settingsStore.ts,
    src/stores/themeStore.ts
  </read_first>
  <action>
1. Update `src/components/canvas/TerminalNode.tsx`:
   - Import `terminalSchemes` from `../../lib/terminalSchemes`
   - Import `useSettingsStore` to get current `colorScheme`
   - When creating/configuring the xterm.js Terminal instance, apply the theme:
     ```typescript
     const colorScheme = useSettingsStore((s) => s.colorScheme);
     // In terminal options or via terminal.options.theme:
     terminal.options.theme = terminalSchemes[colorScheme];
     ```
   - If the xterm instance already exists and colorScheme changes, update via `terminal.options.theme = terminalSchemes[colorScheme]` (xterm.js supports live theme updates)
   - Use a useEffect that watches colorScheme and updates the terminal theme when it changes

2. Update `src-tauri/tauri.conf.json`:
   - Add `"transparent": true` to the window configuration (same level as "decorations")
   - Keep `"decorations": false` as is

3. Update `src/App.tsx` (or the root container):
   - Add CSS to the root app container for rounded corners:
     ```css
     border-radius: 10px;
     overflow: hidden;
     ```
   - Add a subtle CSS box-shadow to replace the native shadow lost from transparency:
     ```css
     box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
     ```
   - These styles go on the outermost div that wraps the entire app
   - Also update theme application to use new `resolvedTheme` (or the alias) from themeStore
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'terminalSchemes' src/components/canvas/TerminalNode.tsx
    - grep -q 'colorScheme' src/components/canvas/TerminalNode.tsx
    - grep -q '"transparent": true' src-tauri/tauri.conf.json
    - grep -q 'border-radius' src/App.tsx
  </acceptance_criteria>
  <done>Terminal tiles use the selected color scheme from settings. Window has transparent background with CSS rounded corners and box-shadow. Theme preference change is reflected immediately.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `cargo check` passes in src-tauri
- Terminal tiles render with One Dark colors by default
- Theme preference System/Dark/Light resolves correctly
- Window config has transparent: true
</verification>

<success_criteria>
- System theme preference is detected on launch (no flicker)
- User has three theme options: System, Dark, Light
- Terminals render with One Dark scheme by default
- Color scheme can be changed to Dracula and all terminals update
- Window has rounded corners on macOS via transparent webview + border-radius
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidebar-session-persistence/02-02-SUMMARY.md`
</output>
