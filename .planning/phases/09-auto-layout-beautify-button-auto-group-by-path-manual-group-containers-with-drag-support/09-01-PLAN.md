---
phase: 09-auto-layout-beautify-button-auto-group-by-path-manual-group-containers-with-drag-support
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/autoLayout.ts
  - src/lib/grouping.ts
  - src/test/autoLayout.test.ts
  - src/test/grouping.test.ts
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - GROUP-01
  - GROUP-02
  - GROUP-03

must_haves:
  truths:
    - "Grid layout positions tiles without overlap"
    - "Grid layout respects pile ordering for placement sequence"
    - "Grid layout snaps positions to GRID_SIZE"
    - "CWD grouping detects 2+ terminals sharing the same cwd"
    - "CWD grouping ignores non-terminal nodes"
    - "Region bounds calculation wraps members with correct padding and header offset"
  artifacts:
    - path: "src/lib/autoLayout.ts"
      provides: "computeGridLayout function"
      exports: ["computeGridLayout"]
    - path: "src/lib/grouping.ts"
      provides: "detectCwdGroups, computeRegionBounds functions"
      exports: ["detectCwdGroups", "computeRegionBounds"]
    - path: "src/test/autoLayout.test.ts"
      provides: "Unit tests for grid layout"
    - path: "src/test/grouping.test.ts"
      provides: "Unit tests for cwd grouping and region bounds"
  key_links:
    - from: "src/lib/autoLayout.ts"
      to: "src/lib/gridSnap.ts"
      via: "imports GRID_SIZE"
      pattern: "import.*GRID_SIZE.*gridSnap"
---

<objective>
Create the pure-logic modules for auto-layout and grouping with full test coverage.

Purpose: These two utility modules (autoLayout.ts, grouping.ts) contain the core algorithms that Plan 02 will wire into the UI. By building them TDD-first, we ensure correct behavior before any UI integration.

Output: Two tested utility modules exporting computeGridLayout, detectCwdGroups, and computeRegionBounds.
</objective>

<execution_context>
@/Users/volodymyrsaakian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/volodymyrsaakian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auto-layout-beautify-button-auto-group-by-path-manual-group-containers-with-drag-support/09-RESEARCH.md

@src/lib/gridSnap.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/gridSnap.ts:
```typescript
export const GRID_SIZE = 20;
export const SNAP_THRESHOLD = 10;
export function magneticSnap(value: number, gridSize?: number, threshold?: number): number;
export function magneticSnapPosition(x: number, y: number, gridSize?: number, threshold?: number): { x: number; y: number; snappedX: boolean; snappedY: boolean };
```

From @xyflow/react (Node type used in both modules):
```typescript
interface Node {
  id: string;
  type?: string;
  position: { x: number; y: number };
  data: Record<string, unknown>;
  style?: { width?: number; height?: number; [key: string]: unknown };
  measured?: { width?: number; height?: number };
  width?: number;
  height?: number;
  zIndex?: number;
}
```
</interfaces>

<tasks>

<feature>
  <name>Feature A: Grid-Packing Layout Algorithm</name>
  <files>src/lib/autoLayout.ts, src/test/autoLayout.test.ts</files>
  <read_first>src/lib/gridSnap.ts, src/test/persistence.test.ts</read_first>
  <behavior>
    - computeGridLayout with 3 tiles of same size (640x480) returns non-overlapping positions in a row
    - computeGridLayout with tiles exceeding maxRowWidth wraps to next row
    - computeGridLayout positions are snapped to GRID_SIZE (20px) multiples
    - computeGridLayout orders tiles according to provided orderedIds (pile order)
    - computeGridLayout with mixed tile sizes (640x480 terminal + 300x300 note) uses tallest tile in row for row height
    - computeGridLayout with empty orderedIds falls back to node array order
    - computeGridLayout skips region nodes (type === "region")
    - Returns Map<string, { x: number; y: number }> keyed by node id
  </behavior>
  <action>
    RED: Create src/test/autoLayout.test.ts with tests for all behaviors above. Use mock Node objects with id, type, position, style.width/height. Import computeGridLayout from "../lib/autoLayout". Run tests -- all fail.

    GREEN: Create src/lib/autoLayout.ts implementing computeGridLayout:
    - Accept (nodes: Node[], orderedIds: string[], options?: { gap?: number; padding?: number })
    - Default gap=40, padding=40
    - Filter out region nodes
    - Sort content nodes by orderedIds position (nodes not in orderedIds go to end)
    - Row-based packing: place tiles left-to-right, wrap when x + width exceeds maxRowWidth
    - maxRowWidth = Math.max(1200, Math.sqrt(orderedNodes.length) * 700)
    - Snap every position to GRID_SIZE using Math.round(val / GRID_SIZE) * GRID_SIZE (import GRID_SIZE from gridSnap)
    - Track rowHeight as tallest tile in current row
    - Return Map<string, { x: number; y: number }>

    REFACTOR: Clean up if needed.
  </action>
  <verify>
    <automated>npx vitest run src/test/autoLayout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "computeGridLayout" src/lib/autoLayout.ts confirms export exists
    - grep -q "GRID_SIZE" src/lib/autoLayout.ts confirms grid snap integration
    - grep -q "region" src/lib/autoLayout.ts confirms region filtering
    - npx vitest run src/test/autoLayout.test.ts exits 0 with all tests passing
  </acceptance_criteria>
  <done>computeGridLayout correctly positions tiles in grid rows, respects pile order, snaps to grid, and skips regions. All tests green.</done>
</feature>

<feature>
  <name>Feature B: CWD Grouping and Region Bounds</name>
  <files>src/lib/grouping.ts, src/test/grouping.test.ts</files>
  <read_first>src/lib/gridSnap.ts, src/components/canvas/RegionNode.tsx, src/test/persistence.test.ts</read_first>
  <behavior>
    - detectCwdGroups with 3 terminals (2 sharing cwd "/foo", 1 with "/bar") returns Map with one entry "/foo" -> [node1, node2]
    - detectCwdGroups ignores non-terminal nodes (notes, images, regions)
    - detectCwdGroups excludes groups with only 1 terminal (minimum 2 to form a group)
    - detectCwdGroups handles terminals with no cwd gracefully (skips them)
    - computeRegionBounds with 2 nodes at (100,100) 640x480 and (800,100) 640x480 returns correct bounding box with padding=20 and headerHeight=32
    - computeRegionBounds padding and headerHeight are configurable with defaults (padding=20, headerHeight=32)
  </behavior>
  <action>
    RED: Create src/test/grouping.test.ts with tests for all behaviors above. Use mock Node objects. Import detectCwdGroups and computeRegionBounds from "../lib/grouping". Run tests -- all fail.

    GREEN: Create src/lib/grouping.ts:

    detectCwdGroups(nodes: Node[]): Map<string, Node[]>
    - Iterate nodes, skip any where node.type !== "terminal"
    - Extract cwd from (node.data as Record<string, unknown>).cwd
    - Skip if cwd is falsy
    - Group by cwd string
    - Delete entries with < 2 members
    - Return the Map

    computeRegionBounds(members: Node[], padding?: number, headerHeight?: number): { x: number; y: number; width: number; height: number }
    - Default padding=20, headerHeight=32
    - Calculate min/max x/y across all members using position + style dimensions (default 640x480)
    - Return { x: minX - padding, y: minY - padding - headerHeight, width: maxX - minX + padding * 2, height: maxY - minY + padding * 2 + headerHeight }

    REFACTOR: Clean up if needed.
  </action>
  <verify>
    <automated>npx vitest run src/test/grouping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "detectCwdGroups" src/lib/grouping.ts confirms export exists
    - grep -q "computeRegionBounds" src/lib/grouping.ts confirms export exists
    - npx vitest run src/test/grouping.test.ts exits 0 with all tests passing
  </acceptance_criteria>
  <done>detectCwdGroups correctly identifies terminal clusters by cwd, and computeRegionBounds calculates correct bounding boxes. All tests green.</done>
</feature>

</tasks>

<verification>
npx vitest run src/test/autoLayout.test.ts src/test/grouping.test.ts
</verification>

<success_criteria>
- computeGridLayout produces non-overlapping, grid-snapped positions respecting pile order
- detectCwdGroups finds terminal clusters with 2+ members sharing cwd
- computeRegionBounds wraps members with correct padding and header offset
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-auto-layout-beautify-button-auto-group-by-path-manual-group-containers-with-drag-support/09-01-SUMMARY.md`
</output>
