---
phase: 01-canvas-terminal-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - vitest.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src/test/setup.ts
  - src/test/scaffold.test.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02

must_haves:
  truths:
    - "Tauri v2 app compiles and opens a window"
    - "All frontend and Rust dependencies are installed"
    - "Vitest is configured and runs with jsdom environment"
    - "TypeScript compiles with no errors"
  artifacts:
    - path: "package.json"
      provides: "All frontend dependencies including @xyflow/react, @xterm/xterm, zustand, Tauri plugins"
    - path: "src-tauri/Cargo.toml"
      provides: "All Rust dependencies including portable-pty, tauri plugins, tokio, serde"
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri config with decorations:false, window sizing"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with jsdom and React plugin"
    - path: "src/test/scaffold.test.ts"
      provides: "Initial test stubs for scaffold verification"
  key_links:
    - from: "vite.config.ts"
      to: "src-tauri/tauri.conf.json"
      via: "Tauri build integration"
      pattern: "vitejs.*react"
---

<objective>
Scaffold the Tauri v2 + React + TypeScript project from scratch, install all dependencies (frontend and Rust), configure the build toolchain, and set up the Vitest test framework with initial test stubs.

Purpose: This is the bare foundation -- everything else depends on the project compiling and tests running.
Output: Compilable Tauri app with all dependencies, Vitest configured, initial test stubs.
</objective>

<execution_context>
@/Users/volodymyrsaakian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/volodymyrsaakian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-terminal-core/01-CONTEXT.md
@.planning/phases/01-canvas-terminal-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri v2 + React project with all dependencies and test framework</name>
  <files>
    package.json, tsconfig.json, tsconfig.node.json, vite.config.ts, vitest.config.ts,
    index.html, src/main.tsx, src/App.tsx, src-tauri/tauri.conf.json, src-tauri/Cargo.toml,
    src-tauri/src/main.rs, src-tauri/src/lib.rs, src-tauri/capabilities/default.json,
    src/test/setup.ts, src/test/scaffold.test.ts
  </files>
  <action>
    Create the Tauri v2 + React + TypeScript project. Use `npm create tauri-app@latest` with React+TypeScript template, OR manually scaffold if the CLI template is outdated. The end result must be:

    1. **Tauri scaffold:** Run `npm create tauri-app@latest excalicode-temp -- --template react-ts` in a temp location, then copy the scaffold into the project root (since root already has .planning/). Alternatively, manually create the structure if cleaner.

    2. **Install frontend deps:**
       ```
       npm install @xyflow/react @xterm/xterm@5.5.0 @xterm/addon-fit @xterm/addon-webgl zustand @tauri-apps/api @tauri-apps/plugin-fs @tauri-apps/plugin-store
       npm install -D @tailwindcss/vite vitest @testing-library/react @testing-library/jest-dom jsdom @types/react @types/react-dom
       ```

    3. **Vite config:** `vite.config.ts` with `@vitejs/plugin-react` and `@tailwindcss/vite` plugins. Set `server.strictPort: true`, `envPrefix: ['VITE_', 'TAURI_']`, and `build.target: ['es2021', 'chrome100', 'safari15']` per Tauri v2 requirements.

    4. **Vitest config:** `vitest.config.ts` with jsdom environment, React plugin, setup file at `src/test/setup.ts`.

    5. **Test setup:** `src/test/setup.ts` importing `@testing-library/jest-dom`.

    6. **Initial test stubs:** `src/test/scaffold.test.ts` with:
       - `it.todo('App component renders without crashing')`
       - `it.todo('ThemeProvider applies CSS variables')`
       - `it.todo('Canvas component mounts with ReactFlow')`
       - `it.todo('canvasStore addTerminalNode creates node')`
       - `it.todo('themeStore toggleTheme switches theme')`
       - `it.todo('useKeyboardShortcuts prevents browser zoom')`
       These are placeholder stubs so vitest reports them. Plan 01-02 will flesh them out.

    7. **Tauri config (`tauri.conf.json`):**
       - `app.windows[0]`: title "Excalicode", `decorations: false`, width 1400, height 900, minWidth 800, minHeight 600
       - Bundle identifier: `com.excalicode.app`
       - Register plugins: `fs` and `store`

    8. **Cargo.toml:** Add dependencies: `tauri` (v2, features: ["protocol-asset"]), `tauri-plugin-fs`, `tauri-plugin-store`, `portable-pty` (0.9), `serde` (features: ["derive"]), `serde_json`, `tokio` (features: ["full"]), `anyhow`, `uuid` (features: ["v4"]). Set edition = "2021".

    9. **Rust entry (`src-tauri/src/main.rs` and `lib.rs`):** Basic Tauri app setup. `main.rs` calls `tauri::Builder::default()` with `.plugin(tauri_plugin_fs::init())` and `.plugin(tauri_plugin_store::Builder::default().build())`. Use the `lib.rs` pattern if Tauri v2 template requires it. No PTY commands yet (Plan 03 handles that).

    10. **Capabilities:** Create `src-tauri/capabilities/default.json` granting `core:window:allow-close`, `core:window:allow-minimize`, `core:window:allow-maximize`, `core:window:allow-start-dragging`, `core:window:allow-set-size`, `fs:default`, `store:default`.

    11. **`src/main.tsx`:** Standard React 19 entry with `createRoot`.

    12. **`src/App.tsx`:** Minimal placeholder component rendering "Excalicode" text. Full app shell comes in Plan 01-02.

    13. **`index.html`:** Standard Tauri template HTML with div#root.

    IMPORTANT: Pin @xterm/xterm to exactly 5.5.0 (NOT v6) per research decision.
    IMPORTANT: Use `@xyflow/react` (NOT the old `reactflow` package).
    IMPORTANT: Do NOT use tauri-plugin-pty -- we use custom portable-pty commands (Plan 03).
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Edu/excalicode && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Tauri app compiles (cargo check passes). TypeScript compiles (tsc --noEmit passes). Vitest runs and reports todo test stubs. All frontend and Rust dependencies installed.</done>
</task>

</tasks>

<verification>
1. `cargo check --manifest-path src-tauri/Cargo.toml` succeeds
2. `npx tsc --noEmit` succeeds
3. `npx vitest run` runs and shows todo stubs
4. `package.json` lists all required frontend dependencies
5. `src-tauri/Cargo.toml` lists all required Rust dependencies
</verification>

<success_criteria>
- Tauri v2 + React project compiles cleanly (Rust and TypeScript)
- All dependencies installed (xyflow, xterm 5.5.0, zustand, tauri plugins, portable-pty)
- Vitest configured with jsdom environment and test stubs reported
- Tauri window config has decorations:false, correct sizing
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-terminal-core/01-01-SUMMARY.md`
</output>
