---
phase: 8
slug: enhanced-ssh-connection-integration
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for the Enhanced SSH Connection Integration phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom inline styles with CSS custom properties) |
| Icon library | Inline SVG (hand-drawn, 14x14 / 16x16 viewBox, 1.5px stroke) |
| Font | system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, button internal padding |
| sm | 8px | Compact element spacing, sidebar section padding, button group gaps, dropdown item vertical padding |
| md-sm | 12px | Dropdown item horizontal padding, badge horizontal padding |
| md | 16px | Default element spacing, sidebar header left padding, empty state padding |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 12px | 400 | 1.5 |
| Label | 11px | 600 | 1.3 |
| Meta | 10px | 400 | 1.3 |
| Heading | 13px | 600 | 1.2 |

Notes:
- Sidebar text uses 12px body, 11px for button labels and tab labels, 10px for secondary meta (e.g. `user@host:port` under connection name).
- Heading (13px 600) used for dropdown section headers like "SSH Config Hosts" and "Saved Connections".
- All sizes match existing sidebar typography observed in SshPanel.tsx, SidebarTabs.tsx, and Sidebar.tsx.
- Only two weights are used: 400 (regular) and 600 (semibold). No intermediate weights.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | var(--bg-primary) | Canvas background, main app surface |
| Secondary (30%) | var(--bg-sidebar), var(--bg-secondary) | Sidebar background, dropdown panels, group headers, SSH quick-connect dropdown |
| Accent (10%) | var(--accent) | SSH connect button icon, active connection indicator, "Connect" CTA, active tab underline, remote project badge background |
| Destructive | N/A (uses window.confirm for delete) | Connection deletion confirmation via native dialog |
| SSH Remote tint | var(--accent) at 15% opacity | Remote file tree left border (3px solid) to distinguish remote from local |

Accent reserved for: SSH connect button highlight, "Connect" action icon, active connection dot, remote project badge pill, active dropdown item hover background

---

## Component Inventory

### New Components

| Component | Location | Description |
|-----------|----------|-------------|
| SshQuickConnect | `src/components/sidebar/SshQuickConnect.tsx` | Dropdown menu triggered by SSH button in sidebar header. Lists SSH config hosts, saved connections, and action links. |
| RemoteFileTree | `src/components/sidebar/RemoteFileTree.tsx` | File tree for remote directories fetched via SSH exec. Shares visual style with FileTree but uses `sshReadRemoteDir` IPC. |

### Modified Components

| Component | Change |
|-----------|--------|
| Sidebar.tsx | Add SSH button (globe/terminal icon) next to Plus icon in header button group. Remove `"ssh"` from activeTab union type. |
| SidebarTabs.tsx | Remove SSH tab from tabs array. Tabs become: Files, Piles, Git (3 tabs). |
| SshPanel.tsx | Repurpose into content rendered inside SshQuickConnect dropdown. Remove standalone panel wrapper. |

### Unchanged Components

SshConnectionForm.tsx remains as-is for manual connection entry (rendered as modal overlay).

---

## Interaction Contracts

### SSH Button in Sidebar Header

- **Placement:** Right side of sidebar header, between view-mode toggle and Plus (open folder) button.
- **Icon:** 14x14 SVG globe with terminal prompt indicator. Stroke: `currentColor`, weight 1.5px.
- **Idle state:** `color: var(--text-secondary)`, background transparent.
- **Hover state:** `background: var(--bg-secondary)`, border-radius 3px.
- **Active (dropdown open):** `background: var(--bg-secondary)`.
- **Click behavior:** Toggles SshQuickConnect dropdown below the button.

### SshQuickConnect Dropdown

- **Position:** Absolute, anchored to SSH button, top: 100%, right: 0, marginTop: 4px.
- **Dimensions:** minWidth: 240px, maxWidth: 320px.
- **Background:** var(--bg-secondary).
- **Border:** 1px solid var(--border), border-radius: 6px.
- **Shadow:** 0 4px 12px rgba(0,0,0,0.3).
- **z-index:** 9999 (matches existing project dropdown pattern).
- **Dismiss:** Click outside closes dropdown (mousedown listener on document).
- **Sections (top to bottom):**
  1. **SSH Config Hosts** -- heading (11px, 600, uppercase, letter-spacing 0.05em, color: var(--text-secondary), padding: 8px 12px). List of host buttons below.
  2. **Saved Connections** -- same heading style. List of saved connection buttons.
  3. **Divider** -- 1px solid var(--border), margin: 4px 8px.
  4. **Actions** -- "+ New Connection..." and "Edit SSH Config" links.

### SSH Config Host Item (in dropdown)

- **Layout:** Single row, padding: 8px 12px.
- **Primary text:** host_alias, 12px 600, color: var(--text-primary).
- **Secondary text:** (hostname or host_alias) in parentheses, 12px 400, color: var(--text-secondary).
- **Hover:** background: var(--accent), color: #fff. Transition: none (instant, matching existing dropdown pattern).
- **Click:** Initiates SSH connection flow. If auth is needed (password), opens SshConnectionForm pre-filled with config host details.

### Saved Connection Item (in dropdown)

- **Layout:** Single row, padding: 8px 12px.
- **Primary text:** connection name, 12px 600, color: var(--text-primary).
- **Secondary text:** `user@host` in parentheses, 12px 400, color: var(--text-secondary).
- **Hover:** Same as config host item.
- **Click:** Spawns SSH terminal tile on canvas at viewport center.

### Remote Project Indicator

- **In project dropdown:** Remote projects display as `user@host:path` format. An "SSH" badge (pill) appears after the project name: background var(--accent), color #fff, font-size 9px, font-weight 600, padding 2px 8px, border-radius 8px.
- **In file tree:** When displaying a remote project, the FileTree area shows a 3px left border in var(--accent) at 40% opacity. A small header bar (padding 4px 8px, font-size 10px, color: var(--text-secondary)) reads "Remote: user@host:path" with a disconnect icon button on the right.

### Remote File Tree

- **Visual style:** Identical to local FileTree (same indent, font size, expand/collapse chevrons).
- **Loading state:** Inline spinner (CSS animation, 12px, color: var(--text-secondary)) centered in tree area with text "Loading remote files...".
- **Error state:** Red-tinted text (not a new color variable -- use inline `color: #ef4444`) with retry link: "Connection lost. Reconnect?"
- **Empty directory:** Italic text, 12px, color: var(--text-secondary): "Empty directory".

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Connect" (on SSH config host items and saved connection items in dropdown) |
| Empty state heading (no SSH config hosts) | "No SSH Config Hosts" |
| Empty state body (no SSH config hosts) | "Add hosts to ~/.ssh/config or create a connection below." |
| Empty state heading (no saved connections) | "No Saved Connections" |
| Empty state body (no saved connections) | "Click '+ New Connection' to add one." |
| Empty state (entire dropdown, no hosts and no connections) | "No SSH connections yet. Add hosts to ~/.ssh/config or click '+ New Connection' to get started." |
| Error: connection failed | "Connection failed: {error}. Check host, credentials, and network." |
| Error: remote file listing failed | "Could not list remote directory. Connection may have dropped. Reconnect?" |
| Error: SSH config parse failed | "Could not parse ~/.ssh/config: {error}. Edit the file to fix syntax." |
| Destructive: delete connection | Uses `window.confirm("Delete connection '{name}'? This cannot be undone.")` (matches existing pattern from Phase 04 stash drop) |
| Action: new connection | "+ New Connection..." |
| Action: edit SSH config | "Edit SSH Config" |
| Remote project header | "Remote: {user}@{host}:{path}" |
| Disconnect button tooltip | "Disconnect from {host}" |

---

## State Contracts

### sshStore Extensions

| State Field | Type | Default | Purpose |
|-------------|------|---------|---------|
| configHosts | SshConfigHost[] | [] | Parsed ~/.ssh/config host entries |
| activeRemoteProject | { sessionId: string; host: string; user: string; remotePath: string } or null | null | Currently browsed remote project |
| loadConfigHosts() | async action | -- | Fetches and parses ~/.ssh/config via IPC |

### projectStore Extensions

| State Field | Type | Default | Purpose |
|-------------|------|---------|---------|
| isRemote (on Project) | boolean | false | Distinguishes remote from local projects |
| sshSessionId (on Project) | string or undefined | undefined | Links remote project to SSH session |
| sshHost (on Project) | string or undefined | undefined | Display label for remote project |

---

## Animation and Transitions

| Element | Property | Duration | Easing |
|---------|----------|----------|--------|
| Dropdown appear | opacity | 0ms (instant) | none |
| Tab underline | border-color | 150ms | ease (matches existing SidebarTabs) |
| Button hover background | background-color | 0ms (instant) | none (matches existing sidebar buttons) |
| Remote file tree loading spinner | rotate | 800ms | linear, infinite |

Notes: The existing codebase uses instant hover transitions (no transition property on sidebar buttons). Maintain this pattern. The only timed transition in the sidebar is the tab underline (150ms).

---

## Accessibility

| Requirement | Implementation |
|-------------|----------------|
| Keyboard navigation | SSH button focusable via Tab. Dropdown items navigable with Arrow keys. Escape closes dropdown. |
| Focus indicator | 2px solid var(--accent) outline on focused interactive elements (offset 1px). |
| Screen reader | SSH button: `aria-label="SSH Connections"`, `aria-expanded={isOpen}`. Dropdown: `role="menu"`. Items: `role="menuitem"`. |
| Color contrast | All text meets WCAG AA against respective backgrounds (verified: #e0e0e0 on #12122a = 10.2:1, #8888aa on #12122a = 4.6:1). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| N/A | none | not applicable (no shadcn, no third-party registries) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
