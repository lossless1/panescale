import { describe, it } from "vitest";

describe("useFocusMode", () => {
  it.todo("enterTerminalMode sets mode and activeId");
  it.todo("exitToCanvasMode resets state");
});

describe("settingsStore", () => {
  it.todo("has correct defaults");
});

describe("canvasStore", () => {
  it.todo("addTerminalNode creates node at position");
  it.todo("removeNode removes node");
  it.todo("bringToFront updates zIndex");
});

describe("TerminalNode", () => {
  it.todo("renders title bar with drag handle");
});

describe("TerminalTitleBar", () => {
  it.todo("close button calls onClose");
});
